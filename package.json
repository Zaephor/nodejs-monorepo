{
  "name": "root",
  "private": true,
  "scripts": {
    "batch-update": "lerna exec --parallel 'ncu -u'",
    "build:todo": "leasot -x --reporter markdown --ignore '**/node_modules/**,**.json' '**/*' > TODO.md && git add TODO.md",
    "commit": "commit",
    "postinstall": "lerna bootstrap --hoist",
    "pretest": "standard",
    "test": "jest"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.0.0",
    "@commitlint/config-conventional": "^8.0.0",
    "@commitlint/config-lerna-scopes": "^8.0.0",
    "@commitlint/prompt-cli": "^8.0.0",
    "husky": "latest",
    "jest": "latest",
    "leasot": "^7.3.4",
    "lerna": "^3.14.1",
    "lint-staged": "^8.1.7",
    "npm-check-updates": "^3.1.10",
    "standard": "latest"
  },
  "standard": {
    "ignore": [
      "**/__tests__/**"
    ],
    "globals": [
      "describe",
      "test",
      "beforeAll",
      "beforeEach",
      "afterAll",
      "afterEach",
      "expect"
    ]
  }
}
